version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  go: "1.25"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unused

formatters:
  enable:
    - gofmt
    - goimports
